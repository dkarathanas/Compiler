print("Evaluating an if/else expr...");

foo = true && false;
d = 0;
if (foo && true || false) {
	++d;
}
else {
	r = d--;
}

print("Evaluating a while statement...");
while(foo && d > 1821) {
	r = --d;
}

print("Evaluating a for statement...");
for (i = 0; i > 1821; i++) {
	if (false) {
		break;
	}
	else
		continue;
	continue;
}

print("And a little if statement just for fun");
if (a&&b&&c&&d&&e&&f&&g&&h&&i&&j&&k&&l&&m&&o&&p&&q&&r&&s&&t&&u&&v&&w&&x&&y&&z){}
